@model IEnumerable<MeetingTracker.Models.MeetingType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MeetingTypeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MeetingTypeDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MeetingTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MeetingTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MeetingTypeId })
        </td>
    </tr>
}

</table>
